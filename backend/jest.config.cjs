module.exports = {
  testEnvironment: "node", // Use Node.js environment for backend tests
  verbose: true, // Enable verbose output for better test results visibility
  testMatch: ["**/test/**/*.test.js"], // Match test files in the test directory
};
